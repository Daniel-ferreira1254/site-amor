<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<title>Para Leda 💖</title>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

<style>
body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(135deg, #ff758c, #ff7eb3);
  font-family: "Poppins", sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden;
  color: white;
}

.container {
  text-align: center;
  animation: fadeIn 1s ease;
}

h1 {
  font-size: 2.5rem;
}

.question {
  font-size: 2rem;
  margin: 20px 0;
}

img.gif {
  width: 220px;
  border-radius: 12px;
}

.button-container {
  position: relative;
  width: 300px;
  margin: 30px auto;
}

.btn {
  border: none;
  border-radius: 50px;
  padding: 12px 28px;
  font-size: 16px;
  cursor: pointer;
  background: #ff4d6d;
  color: white;
  box-shadow: 0 5px 15px rgba(0,0,0,0.2);
  transition: 0.3s;
}

.btn:hover {
  transform: scale(1.1);
}

#yes {
  position: absolute;
  left: 0;
}

#no {
  position: absolute;
  right: 0;
}

@keyframes fadeIn {
  from {opacity: 0; transform: translateY(20px);}
  to {opacity: 1; transform: translateY(0);}
}

/* corações iniciais */
.heart {
  position: absolute;
  color: white;
  animation: float 4s linear infinite;
  font-size: 20px;
}

@keyframes float {
  from {transform: translateY(100vh);}
  to {transform: translateY(-10vh);}
}
</style>
</head>

<body>

<audio autoplay loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3">
</audio>

<div class="container">
  <h1>💖 Leda Constância 💖</h1>

  <img class="gif" src="https://media.tenor.com/2roX3uxz_68AAAAC/cute-love.gif">

  <div class="question">Você me ama? 🥺</div>

  <div class="button-container">
    <button class="btn" id="yes" onclick="sim()">Sim 💘</button>
    <button class="btn" id="no">Não 😢</button>
  </div>
</div>

<script>
// botão NÃO fugindo
const noBtn = document.getElementById("no");

noBtn.addEventListener("mouseover", () => {
  const x = Math.random() * (window.innerWidth - 120);
  const y = Math.random() * (window.innerHeight - 60);

  noBtn.style.left = x + "px";
  noBtn.style.top = y + "px";
});

// função SIM (FINAL)
function sim() {
  document.body.innerHTML = `
    <style>
      body {
        margin:0;
        overflow:hidden;
        background: linear-gradient(135deg, #ff758c, #ff7eb3);
        font-family: Poppins;
        color:white;
      }

      .box {
        text-align:center;
        padding:20px;
        animation: fadeIn 1s ease;
      }

      .slider img {
        width:250px;
        border-radius:15px;
        position:absolute;
        left:50%;
        transform:translateX(-50%);
        opacity:0;
        transition: opacity 1s;
      }

      .active {
        opacity:1 !important;
      }

      .heart {
        position:absolute;
        animation: fall 4s linear infinite;
        font-size:20px;
      }

      @keyframes fall {
        from {transform:translateY(-10vh);}
        to {transform:translateY(100vh);}
      }
    </style>

    <div class="box">
      <h1>Eu sabia 😍💖</h1>
      <p style="font-size:20px;">Você me faz muito feliz ❤️</p>

      <div class="slider">
        <img src="foto1.jpeg" class="slide active">
        <img src="foto2.jpeg" class="slide">
        <img src="foto3.jpeg" class="slide">
      </div>
    </div>
  `;

  // slideshow
  let index = 0;
  const slides = document.querySelectorAll(".slide");

  setInterval(() => {
    slides[index].classList.remove("active");
    index = (index + 1) % slides.length;
    slides[index].classList.add("active");
  }, 2000);

  // corações caindo
  function createHeart() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "💖";
    heart.style.left = Math.random() * 100 + "vw";

    document.body.appendChild(heart);

    setTimeout(() => heart.remove(), 4000);
  }

  setInterval(createHeart, 200);
}

// corações na tela inicial
function createHeartStart() {
  const heart = document.createElement("div");
  heart.classList.add("heart");
  heart.innerHTML = "💖";
  heart.style.left = Math.random() * 100 + "vw";

  document.body.appendChild(heart);

  setTimeout(() => heart.remove(), 4000);
}

setInterval(createHeartStart, 300);
</script>

</body>
</html>